[package]
name = "rzip"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.59", features = ["derive"] }
num_cpus = "1.17.0"
zstd = { version = "0.13.3", features = ["zstdmt"] }
